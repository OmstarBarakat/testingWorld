<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.testinglab.mytgcompination">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/auto_notify" />

        <activity
            android:name=".MainActivity"
            android:label="@string/main_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".settingActivity" />
        <activity
            android:name=".FeedNewsActivity"
            android:label="@string/label_latest_news"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".NotifyServiceActivity"
            android:label="@string/label_notify_service"
            android:parentActivityName=".MainActivity" />

        <service android:name="org.testinglab.mytgcompination.service_receiver.MessagingService"></service>

        <receiver android:name="org.testinglab.mytgcompination.service_receiver.MessageReadReceiver">
            <intent-filter>
                <action android:name="com.example.android.messagingservice.ACTION_MESSAGE_READ" />
            </intent-filter>
        </receiver>

        <receiver android:name="org.testinglab.mytgcompination.service_receiver.MessageReplyReceiver">
            <intent-filter>
                <action android:name="com.example.android.messagingservice.ACTION_MESSAGE_REPLY" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
